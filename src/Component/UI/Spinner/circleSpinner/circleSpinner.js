import React from 'react'

import classes from './circleSpinner.css'

const circleSpinner = () => {
    return (
        <div className={classes.Loader}>Loading...</div>
    )
}

export default circleSpinner